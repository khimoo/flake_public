# CLAUDE.md

## プロジェクト概要

NixOS の設定を管理する Nix Flake リポジトリ。NixOS のシステム設定と home-manager によるユーザー環境設定を含む。

### 構成

- `flake.nix` — エントリポイント。`nixosConfigurations`（NixOS用）と `homeConfigurations`（スタンドアロン用）を定義
- `hosts/` — ホスト固有の設定（hardware.nix, default.nix, home.nix 等）
- `modules/nixos/` — NixOS モジュール（OS レベルの設定）
- `modules/home-manager/` — home-manager モジュール（ユーザー環境の設定）
  - `modules/home-manager/dev/neovim/config/` — Neovim の lua 設定。mkOutOfStoreSymlink でリポジトリを `~/.config/nvim` に直接 symlink するため、編集が rebuild なしで反映される
  - `modules/home-manager/gui/wezterm/wezterm.lua` — wezterm の lua 設定。同じく mkOutOfStoreSymlink で `~/.config/wezterm/wezterm.lua` に直接 symlink

### ビルド・適用コマンド

```sh
# NixOS 上（OS + home 環境を一括適用）
sudo nixos-rebuild switch --flake .#hostname

# 非 NixOS 環境（home 環境のみ適用）
home-manager switch --flake .#username-host
```

### Nix の純粋性から外れている要素

以下は Nix store 外に命令的にインストールされる impure な要素。nixpkgs でのパッケージングが困難なため、やむを得ず `home.activation` で管理している。

| 要素 | インストール先 | 理由 |
|------|---------------|------|
| rustowl (プリビルドバイナリ + ランタイムダウンロードの sysroot) | `~/.local/share/rustowl/` | 特定の nightly Rust sysroot を必要とし、nixpkgs でのパッケージングが困難 |
| rustowl (symlink) | `~/.local/bin/` | 上記バイナリへのシンボリックリンク |

詳細は `modules/home-manager/dev/rustowl.nix` の `home.activation.rustowl` を参照。

### TODO: デスクトップ側の Firefox プロファイルリネーム（帰宅後に実施）

ラップトップ側では `gbvj989j.default`（デスクトップのプロファイル）がハードコードされていたが、
`firefox.nix` の `path` 指定を削除し（home-manager のデフォルト `"default"` を使用）、
ラップトップのプロファイルを `default` にリネームして対応済み。
デスクトップ側でも以下の手順を実施すること：

1. Firefox を完全に終了する
2. プロファイルをリネーム:
   ```sh
   mv ~/.mozilla/firefox/gbvj989j.default ~/.mozilla/firefox/default
   ```
3. 起動キャッシュを削除（旧パスが記録されており拡張機能が読み込めなくなるため）:
   ```sh
   rm ~/.mozilla/firefox/default/addonStartup.json.lz4
   ```
4. rebuild（Nix 設定はすでに更新済み）:
   ```sh
   sudo nixos-rebuild switch --flake .#nixos-desktop
   ```
5. Firefox を起動して拡張機能が正常に使えることを確認
6. 確認できたらこの TODO を CLAUDE.md から削除する

---

## コード編集のルール

- **ユーザーが明示的に指示するまでコード編集をしないこと**
- 提案・調査・説明は自由に行ってよいが、ファイルの変更は指示を受けてから実行する
- 「やりますか？」と確認し、承認を得てから編集する
- **変更が正しく動作することを検証するまで、別の方針への切り替え（既存コードの削除・書き換え）をしないこと**
  - 問題が発生した場合、まず原因を十分に調査・検証する
  - 検証が不十分なまま「この方式はダメだ」と判断してコードを削除・巻き戻ししない
  - rebuild 後の再起動など、設定反映に必要な手順を踏んだか確認する

### 調査が大変そうなときは積極的にネット検索を使うこと

ローカルでの探索（`nix-instantiate`, `grep`, `find` 等）に何分もかけるよりも、
同じ問題に既に遭遇している人が NixOS Discourse / GitHub Issues / nixpkgs リポジトリ
等にいる可能性が高い。特に以下のような状況では **まず web 検索を試すこと**:

- nixpkgs/home-manager のエラーメッセージそのままで検索すると当事者の議論が見つかりやすい
- 「どのパッケージが X に依存しているか」のような依存逆引き
- nixpkgs のリリースノートや破壊的変更
- ある言語/ツールのベストプラクティス調査
- 「ツール X が **期待通りに動作しない**」(例: DAW がプラグインを認識しない、エディタが LSP を見つけない) のような **ツールの挙動に NixOS 特有の事情が絡む** ケース。複数パッケージを個別検証する前にまずツール側の仕様/既知の振る舞いを検索する

`nix-instantiate --show-trace` で 10 分以上かかる evaluation を走らせるくらいなら、
最初の 1〜2 分で web 検索 → 当たりが付いたら局所的に grep/eval で確認、の順が早い。

**特にアンチパターン**: 「ツールが N 個のパッケージを認識しない」現象に対して、
各パッケージを `nix eval` で個別検証していくこと。N に比例してムダ時間が伸びる。
ツール側の仕様（環境変数を読むか、ハードコードパスか、設定ファイル経由か）を
30 秒 web で調べる方が早い。

### システム適用コマンドは AI が実行しないこと

判断基準は「root 権限が要るか」ではなく **「失敗したときの判断を誰がするか」**。

以下は **AI が実行してはならない**。ユーザーが手動で実行する：

- `sudo nixos-rebuild switch ...` / `nixos-rebuild ...`
- `home-manager switch ...`
- その他、システム/ユーザー環境の世代を切り替える系のコマンド全般

理由: 適用が失敗したとき、世代を戻すか原因を追うかを決めるのはユーザーであり、
その判断を AI に委ねられない。認証が通るようになってもこの制約は残る。
AI は設定ファイルの編集までで止め、「rebuild してください」と依頼する形にする。

`nix flake check`, `nix build`, `nix eval` 等の **副作用のない検証コマンドは実行してよい**。

### root 権限が要るだけの操作は pkexec で実行してよい

世代の切り替えを伴わず、単に root 権限が必要なだけの操作（`mkdir`/`chown`/`mount`/
root 所有ファイルの読み取り等）は、**AI が `pkexec` 経由で実行してよい**。

```sh
pkexec /run/current-system/sw/bin/bash -c 'mkdir -p /mnt/backup/foo && chown pomu:users /mnt/backup/foo'
```

`sudo` は端末を持たない AI のシェルからパスワードを要求できないため使わない。
`sudo -S` でパスワードを渡すのも禁止（会話ログとタスク出力に平文で残る）。

polkit のポリシーは `auth_admin`（`_keep` ではない）なので、呼び出しごとに毎回
パスワードダイアログが出る。ただしダイアログは実行内容を表示せず「本人が座っているか」
しか確認しないため、**実質的な審査点は Claude Code の許可プロンプト側**になる。
AI は何を root で実行するかを事前に明示してから呼び出すこと。

---

## コード品質の指針

凝集度・結合度・Clean Architecture の考え方に基づき、良いコードを心掛ける。

### 凝集度（モジュール内の協調度。高いほど良い）

| レベル | 凝集度 | 説明 | 方針 |
|--------|--------|------|------|
| 1 (最低) | 偶発的凝集 | 無関係な処理の寄せ集め。「とりあえず動く」状態 | **必ず避ける** |
| 2 | 論理的凝集 | フラグで動作を切り替える共通化 | **可能な限り避ける** |
| 3 | 時間的凝集 | 同時期に実行する処理の集合（初期化等）。順序入替可 | できるだけ小さく保つ |
| 4 | 手続き的凝集 | 順番に意味のある処理の集合 | できるだけ小さく保つ |
| 5 | 通信的凝集 | 同じデータを扱う処理の集合。順序は不問 | 許容 |
| 6 | 逐次的凝集 | 前の出力が次の入力となる処理の集合 | 良い |
| 7 (最高) | 機能的凝集 | 単一の明確なタスクを実現し、これ以上分解できない | **目指すべき状態** |

- 凝集度はモジュール内の**一番低いレベル**で評価する
- 関数分割のしすぎは認知負荷を上げる。意味のわかる単位で区切ること

### 結合度（モジュール間の依存度。低いほど良い）

| レベル | 結合度 | 説明 | 方針 |
|--------|--------|------|------|
| 1 (最高) | 内部結合 | 非公開データへの直接アクセス（リフレクション等） | **必ず避ける** |
| 2 | 共通結合 | 複数モジュールが同じグローバルデータにアクセス | **避ける** |
| 3 | 外部結合 | 標準化インターフェース経由でのグローバルアクセス | やむを得ない場合のみ |
| 4 | 制御結合 | フラグで他モジュールの処理フローを制御 | **避ける** |
| 5 | スタンプ結合 | 構造体/クラス全体の受け渡し（不要データを含みうる） | 必要最小限に |
| 6 | データ結合 | プリミティブ型など必要最小限のデータの受け渡し | 良い |
| 7 (最低) | メッセージ結合 | 引数なしのやりとり（タイミングのみ伝達） | **理想的** |

- 凝集度が高くなれば結合度は低くなる傾向がある

### Clean Architecture

- レイヤーに分離し**関心事の分離**を行う
- 依存は**内側（ビジネスロジック）にのみ**向ける。外側（UI、DB、フレームワーク）に依存しない
- 境界を超えるデータは単純なデータ構造を使い、内側が外側の知識を持たないようにする
- 内側から外側への情報伝達にはストリームや依存関係逆転（インターフェース）を使う

### エラーハンドリング

- 適切にエラーハンドリングすること
- 安易にフォールバックやサイレント無視をしない。warning や error を明示的に出すべきタイミングを見極める
- 問題を隠蔽するコードより、問題を早期に発見できるコードを書く

### ドキュメントの同期

- 設定ファイル（プラグインの追加・削除・キーバインド変更等）を変更した場合、`modules/home-manager/dev/neovim/config/docs/` 以下の関連ドキュメントも必ず同時に更新すること
- 特に `docs/README.md` のプラグイン一覧・ワークフロー例・キーバインド表は実態と乖離しやすいので注意
- プラグインを削除した場合、そのプラグインへの参照がドキュメント全体に残っていないか確認すること

### ドキュメントの種類と配置

ドキュメントは**使い方**と**実装方針**を分離して管理する:

| 種類 | 配置先 | 内容 |
|------|--------|------|
| 使い方ガイド | `docs/howtouse/` | 設定の使い方・追加手順・属性一覧など、ユーザー向けの情報 |
| 実装方針 | `docs/architecture/` | 設計判断・なぜそうしたか・トレードオフなど、開発者向けの情報 |
| Neovim 固有 | `modules/home-manager/dev/neovim/config/docs/` | プラグイン・キーバインド・ワークフロー |

- 新しい機能のドキュメントを書く際は、使い方と実装方針を同じファイルに混ぜない
- 各ドキュメントから対になるドキュメントへ相互リンクする（例: 使い方ガイドから実装方針へ、またはその逆）
- 目次: [docs/howtouse/README.md](docs/howtouse/README.md), [docs/architecture/README.md](docs/architecture/README.md)
